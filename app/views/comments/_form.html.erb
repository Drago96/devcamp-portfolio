<%= form_with(model: @comment, url: '#', id: 'comment_form') do |form| %>
  <div class="form-group">
    <%= form.label :content, 'Comment' %>
    <%= form.text_area :content, class: 'form-control' %>
  </div>
  <%= form.submit 'Post Comment', class: 'btn btn-primary' %>
<% end %>
